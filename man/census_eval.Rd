% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/eval-solver.R
\name{census_eval}
\alias{census_eval}
\title{Run censuschat evaluation}
\usage{
census_eval(
  dataset = NULL,
  provider = NULL,
  epochs = 1,
  view = interactive(),
  log_dir = "censuschat_eval_logs"
)
}
\arguments{
\item{dataset}{Path to CSV or tibble with \code{input} and \code{target} columns.
If NULL, uses built-in evaluation dataset.}

\item{provider}{LLM provider to evaluate. If NULL, uses package default.}

\item{epochs}{Number of times to repeat each sample.}

\item{view}{Open vitals viewer after evaluation?}

\item{log_dir}{Directory to save evaluation logs. Defaults to "censuschat_eval_logs"
in current working directory (created if needed). Set to NULL to use vitals default.}
}
\value{
The vitals Task object (invisibly).
}
\description{
Evaluates censuschat accuracy using vitals framework.
}
\examples{
\dontrun{
task <- census_eval()
task$get_samples()
task$metrics
}

}
